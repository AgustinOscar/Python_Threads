import threading
import random

#Función que se ejecuta para escribir un valor en un fichero.
def write_value(author, value):
    pass
    
# Se crea el objeto Lock.
lock_access_file = thrading.Lock()

#Se crean los hilos. 